import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const SeatColorsScene: React.FC<DemoProps> = () => (
  <GuideScene id="SeatColors" step={5} label="좌석 색">
    <></>
  </GuideScene>
);
