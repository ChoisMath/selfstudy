import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const LongPressScene: React.FC<DemoProps> = () => (
  <GuideScene id="LongPress" step={6} label="꾹 눌러 활성화">
    <></>
  </GuideScene>
);
