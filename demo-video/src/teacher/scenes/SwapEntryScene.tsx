import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const SwapEntryScene: React.FC<DemoProps> = () => (
  <GuideScene id="SwapEntry" step={10} label="감독일정">
    <></>
  </GuideScene>
);
