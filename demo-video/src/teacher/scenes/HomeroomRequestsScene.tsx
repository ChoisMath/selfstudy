import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const HomeroomRequestsScene: React.FC<DemoProps> = () => (
  <GuideScene id="HomeroomRequests" step={18} label="불참신청 관리">
    <></>
  </GuideScene>
);
