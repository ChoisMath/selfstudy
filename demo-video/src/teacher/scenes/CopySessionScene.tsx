import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const CopySessionScene: React.FC<DemoProps> = () => (
  <GuideScene id="CopySession" step={4} label="오후1 복사">
    <></>
  </GuideScene>
);
