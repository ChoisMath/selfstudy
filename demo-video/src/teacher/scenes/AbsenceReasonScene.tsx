import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const AbsenceReasonScene: React.FC<DemoProps> = () => (
  <GuideScene id="AbsenceReason" step={17} label="불참사유등록">
    <></>
  </GuideScene>
);
