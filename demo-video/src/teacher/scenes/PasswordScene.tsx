import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const PasswordScene: React.FC<DemoProps> = () => (
  <GuideScene id="Password" step={19} label="비밀번호">
    <></>
  </GuideScene>
);
