import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const HomeroomWeeklyScene: React.FC<DemoProps> = () => (
  <GuideScene id="HomeroomWeekly" step={14} label="주간 출결">
    <></>
  </GuideScene>
);
