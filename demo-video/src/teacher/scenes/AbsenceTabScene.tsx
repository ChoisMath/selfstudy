import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const AbsenceTabScene: React.FC<DemoProps> = () => (
  <GuideScene id="AbsenceTab" step={8} label="불참신청 승인">
    <></>
  </GuideScene>
);
