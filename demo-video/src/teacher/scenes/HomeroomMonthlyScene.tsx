import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const HomeroomMonthlyScene: React.FC<DemoProps> = () => (
  <GuideScene id="HomeroomMonthly" step={15} label="월간출결">
    <></>
  </GuideScene>
);
