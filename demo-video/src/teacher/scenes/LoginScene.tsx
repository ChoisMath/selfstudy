import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const LoginScene: React.FC<DemoProps> = () => (
  <GuideScene id="Login" step={1} label="로그인">
    <></>
  </GuideScene>
);
