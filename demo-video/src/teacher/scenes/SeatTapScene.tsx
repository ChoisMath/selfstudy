import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const SeatTapScene: React.FC<DemoProps> = () => (
  <GuideScene id="SeatTap" step={3} label="출석 체크">
    <></>
  </GuideScene>
);
