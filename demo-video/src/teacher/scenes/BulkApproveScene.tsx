import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const BulkApproveScene: React.FC<DemoProps> = () => (
  <GuideScene id="BulkApprove" step={9} label="일괄승인">
    <></>
  </GuideScene>
);
