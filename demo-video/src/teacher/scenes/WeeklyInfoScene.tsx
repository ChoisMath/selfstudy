import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const WeeklyInfoScene: React.FC<DemoProps> = () => (
  <GuideScene id="WeeklyInfo" step={7} label="주간 정보">
    <></>
  </GuideScene>
);
