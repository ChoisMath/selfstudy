import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const AttendanceTourScene: React.FC<DemoProps> = () => (
  <GuideScene id="AttendanceTour" step={2} label="출석부 화면">
    <></>
  </GuideScene>
);
