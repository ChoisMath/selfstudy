import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const HomeroomTourScene: React.FC<DemoProps> = () => (
  <GuideScene id="HomeroomTour" step={13} label="담임 메뉴">
    <></>
  </GuideScene>
);
