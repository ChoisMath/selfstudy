import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const SwapTotalsScene: React.FC<DemoProps> = () => (
  <GuideScene id="SwapTotals" step={12} label="감독 누계">
    <></>
  </GuideScene>
);
