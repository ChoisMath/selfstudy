import React from "react";
import { GuideScene } from "../../guide/GuideScene";
import type { DemoProps } from "../../props";

export const SwapModalScene: React.FC<DemoProps> = () => (
  <GuideScene id="SwapModal" step={11} label="감독 교체">
    <></>
  </GuideScene>
);
