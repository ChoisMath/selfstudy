# /help 사용설명서 페이지 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인 없이 접근 가능한 `/help` 사용설명서 페이지를 구현한다.

**Architecture:** 미들웨어에 `/help` 공개 경로를 추가하고, `src/app/help/page.tsx` 단일 클라이언트 컴포넌트로 전체 도움말 UI를 구현한다. 4개 섹션(설치/로그인, 감독교사, 담임교사, 학년관리)을 단일 스크롤 페이지에 배치하고, 우측 오버레이 목차로 네비게이션한다.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind CSS 4

**Spec:** `docs/superpowers/specs/2026-04-09-help-page-design.md`

---

### Task 1: 미들웨어에 /help 공개 경로 추가

**Files:**
- Modify: `src/middleware.ts:9-14`

- [ ] **Step 1: 미들웨어 수정**

`src/middleware.ts`의 공개 경로 조건에 `/help`를 추가한다:

```typescript
  // 공개 경로
  if (
    pathname.startsWith("/login") ||
    pathname.startsWith("/api/auth") ||
    pathname.startsWith("/help")
  ) {
    return NextResponse.next();
  }
```

- [ ] **Step 2: 커밋**

```bash
git add src/middleware.ts
git commit -m "feat: /help 공개 경로 추가"
```

---

### Task 2: /help 페이지 구현

**Files:**
- Create: `src/app/help/page.tsx`

- [ ] **Step 1: 페이지 파일 생성 — 기본 구조 + 헤더 + 사이드바**

`src/app/help/page.tsx`를 생성한다. 아래는 전체 파일의 상단 구조 부분이다.

```tsx
"use client";

import { useState, useEffect, useRef, useCallback } from "react";

// 목차 데이터 구조
const TOC = [
  {
    id: "install",
    title: "설치 / 로그인",
    items: [
      { id: "install-android", title: "앱 설치 (안드로이드)" },
      { id: "install-ios", title: "앱 설치 (iOS)" },
      { id: "login", title: "로그인 방법" },
      { id: "password", title: "비밀번호 변경" },
    ],
  },
  {
    id: "supervisor",
    title: "감독교사",
    items: [
      { id: "attendance-check", title: "출석 체크 방법" },
      { id: "absence-approve", title: "불참신청 승인" },
      { id: "supervisor-change", title: "감독교사 변경" },
    ],
  },
  {
    id: "homeroom",
    title: "담임교사",
    items: [
      { id: "weekly-attendance", title: "주간 출결 확인" },
      { id: "monthly-attendance", title: "월간 출결 / Excel / 시수누계" },
      { id: "participation-manage", title: "참여학생 관리" },
      { id: "absence-reason", title: "불참사유 등록" },
      { id: "homeroom-absence-approve", title: "불참신청 승인" },
      { id: "homeroom-schedule", title: "감독일정 변경" },
    ],
  },
  {
    id: "grade-admin",
    title: "학년관리",
    items: [
      { id: "today-dashboard", title: "오늘출결 대시보드" },
      { id: "student-manage", title: "학생관리" },
      { id: "participation-setting", title: "참여설정" },
      { id: "seating", title: "좌석배치" },
      { id: "supervisor-assign", title: "감독배정" },
      { id: "grade-monthly", title: "월간출결 / Excel 다운로드" },
    ],
  },
];

export default function HelpPage() {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [activeSection, setActiveSection] = useState("install");
  const sectionRefs = useRef<Record<string, HTMLElement | null>>({});

  // IntersectionObserver로 현재 섹션 감지
  useEffect(() => {
    const observers: IntersectionObserver[] = [];
    const allIds = TOC.flatMap((s) => [s.id, ...s.items.map((i) => i.id)]);

    allIds.forEach((id) => {
      const el = sectionRefs.current[id];
      if (!el) return;
      const obs = new IntersectionObserver(
        ([entry]) => {
          if (entry.isIntersecting) setActiveSection(id);
        },
        { rootMargin: "-80px 0px -60% 0px", threshold: 0 }
      );
      obs.observe(el);
      observers.push(obs);
    });

    return () => observers.forEach((o) => o.disconnect());
  }, []);

  const scrollTo = useCallback((id: string) => {
    sectionRefs.current[id]?.scrollIntoView({ behavior: "smooth" });
    setSidebarOpen(false);
  }, []);

  const setRef = useCallback((id: string) => (el: HTMLElement | null) => {
    sectionRefs.current[id] = el;
  }, []);

  // 활성 섹션이 어떤 대분류에 속하는지 확인
  const isInGroup = (groupId: string) => {
    if (activeSection === groupId) return true;
    const group = TOC.find((s) => s.id === groupId);
    return group?.items.some((i) => i.id === activeSection) ?? false;
  };

  return (
    <div className="min-h-screen bg-[#f9fafb]">
      {/* 헤더 */}
      <header className="fixed top-0 left-0 right-0 z-40 bg-[#1e40af] text-white">
        <div className="flex items-center justify-between px-4 h-14">
          <div className="flex items-center gap-2">
            <img src="/posan.svg" alt="포산고" className="w-7 h-7" />
            <span className="font-bold text-sm">포산고 자율학습 안내</span>
          </div>
          <button
            onClick={() => setSidebarOpen(!sidebarOpen)}
            className="w-9 h-9 flex items-center justify-center rounded-lg hover:bg-white/10 transition-colors text-xl"
            aria-label="목차 열기"
          >
            {sidebarOpen ? "✕" : "☰"}
          </button>
        </div>
      </header>

      {/* 오버레이 사이드바 */}
      {sidebarOpen && (
        <div
          className="fixed inset-0 z-30 bg-black/30"
          style={{ top: "3.5rem" }}
          onClick={() => setSidebarOpen(false)}
        />
      )}
      <nav
        className={`fixed top-14 right-0 bottom-0 z-30 w-[280px] max-w-[320px] bg-white shadow-xl overflow-y-auto transition-transform duration-300 ${
          sidebarOpen ? "translate-x-0" : "translate-x-full"
        }`}
      >
        <div className="p-4">
          <h2 className="font-bold text-[#1e40af] mb-3 text-sm">목차</h2>
          {TOC.map((section) => (
            <div key={section.id} className="mb-3">
              <button
                onClick={() => scrollTo(section.id)}
                className={`block w-full text-left text-sm font-semibold py-1 px-2 rounded transition-colors ${
                  isInGroup(section.id)
                    ? "text-[#1e40af] bg-[#eff6ff]"
                    : "text-[#374151] hover:bg-[#f3f4f6]"
                }`}
              >
                {section.title}
              </button>
              <div className="ml-3 mt-1">
                {section.items.map((item) => (
                  <button
                    key={item.id}
                    onClick={() => scrollTo(item.id)}
                    className={`block w-full text-left text-xs py-1 px-2 rounded transition-colors ${
                      activeSection === item.id
                        ? "text-[#2563eb] font-medium bg-[#eff6ff]"
                        : "text-[#6b7280] hover:text-[#374151] hover:bg-[#f9fafb]"
                    }`}
                  >
                    {item.title}
                  </button>
                ))}
              </div>
            </div>
          ))}
        </div>
      </nav>

      {/* 본문 */}
      <main className="pt-14 px-4 pb-16 max-w-2xl mx-auto">
        {/* === 섹션 1: 설치 / 로그인 === */}
        {/* ... 아래 Step 2에서 작성 */}
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 본문 콘텐츠 — 섹션 1 (설치 / 로그인)**

`<main>` 안에 첫 번째 섹션을 작성한다. 헬퍼 컴포넌트 `SectionHeader`와 `Step`을 페이지 내부 또는 파일 상단에 정의한다.

```tsx
// 페이지 파일 상단에 헬퍼 컴포넌트 정의 (HelpPage 함수 위)

function SectionHeader({ id, title, refFn }: { id: string; title: string; refFn: (el: HTMLElement | null) => void }) {
  return (
    <div ref={refFn} id={id} className="scroll-mt-16">
      <div className="bg-[#eff6ff] border-l-[3px] border-[#2563eb] px-4 py-3 rounded-r-lg mb-4">
        <h2 className="font-bold text-[#1e40af] text-base">{title}</h2>
      </div>
    </div>
  );
}

function SubHeader({ id, title, refFn }: { id: string; title: string; refFn: (el: HTMLElement | null) => void }) {
  return (
    <h3 ref={refFn} id={id} className="scroll-mt-16 font-semibold text-[#111827] text-sm mt-6 mb-3">
      {title}
    </h3>
  );
}

function StepItem({ num, children }: { num: number; children: React.ReactNode }) {
  return (
    <div className="flex items-start gap-3 mb-2">
      <span className="flex-shrink-0 w-5 h-5 rounded-full bg-[#dbeafe] text-[#1e40af] text-xs flex items-center justify-center font-semibold">
        {num}
      </span>
      <span className="text-sm text-[#374151] leading-relaxed">{children}</span>
    </div>
  );
}

function ColorChip({ color, label }: { color: string; label: string }) {
  return (
    <span className="inline-flex items-center gap-1.5 mr-3 mb-1">
      <span className="w-4 h-4 rounded" style={{ backgroundColor: color }} />
      <span className="text-sm text-[#374151]">{label}</span>
    </span>
  );
}
```

본문의 섹션 1 내용 (`<main>` 내부):

```tsx
{/* === 섹션 1: 설치 / 로그인 === */}
<section className="py-6">
  <SectionHeader id="install" title="📱 설치 / 로그인" refFn={setRef("install")} />

  <SubHeader id="install-android" title="앱 설치 (안드로이드)" refFn={setRef("install-android")} />
  <p className="text-sm text-[#6b7280] mb-3">갤럭시 등 안드로이드 기종은 Chrome 브라우저를 사용합니다.</p>
  <StepItem num={1}>Chrome 브라우저를 엽니다.</StepItem>
  <StepItem num={2}>주소창에 <code className="bg-[#f3f4f6] px-1.5 py-0.5 rounded text-xs font-mono">posan.up.railway.app</code> 을 입력합니다.</StepItem>
  <StepItem num={3}>우측 상단의 <strong>점 3개(⋮)</strong> 를 클릭합니다.</StepItem>
  <StepItem num={4}><strong>"홈 화면에 추가"</strong> → <strong>"설치"</strong> 를 누릅니다.</StepItem>
  <StepItem num={5}>1~2분 후 홈 화면에 앱 아이콘이 생성됩니다.</StepItem>

  <SubHeader id="install-ios" title="앱 설치 (iOS / 아이폰)" refFn={setRef("install-ios")} />
  <p className="text-sm text-[#6b7280] mb-3">아이폰, 아이패드는 Safari 브라우저를 사용합니다.</p>
  <StepItem num={1}>Safari 브라우저를 엽니다.</StepItem>
  <StepItem num={2}>주소창에 <code className="bg-[#f3f4f6] px-1.5 py-0.5 rounded text-xs font-mono">posan.up.railway.app</code> 을 입력합니다.</StepItem>
  <StepItem num={3}>화면 하단(또는 상단)의 <strong>공유 아이콘(⎋)</strong> 을 클릭합니다.</StepItem>
  <StepItem num={4}><strong>"홈 화면에 추가"</strong> → <strong>"추가"</strong> 를 누릅니다.</StepItem>
  <StepItem num={5}>1~2분 후 홈 화면에 앱 아이콘이 생성됩니다.</StepItem>

  <SubHeader id="login" title="로그인 방법" refFn={setRef("login")} />
  <div className="bg-white rounded-lg border border-[#e5e7eb] p-4 mb-3 space-y-2">
    <div className="text-sm">
      <span className="font-semibold text-[#111827]">교사 로그인</span>
      <span className="text-[#6b7280]"> — ID: 개인 NEIS 아이디 / PW: 초기 비밀번호 <code className="bg-[#f3f4f6] px-1 rounded text-xs">1234</code></span>
    </div>
    <div className="text-sm">
      <span className="font-semibold text-[#111827]">학생 로그인</span>
      <span className="text-[#6b7280]"> — 이름 + 학번 5자리 (예: 김서현 / 10101)</span>
    </div>
  </div>

  <SubHeader id="password" title="비밀번호 변경" refFn={setRef("password")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p>로그인 후 우측 상단의 <strong>"담임교사"</strong> 또는 <strong>"감독"</strong> 버튼을 통해 이동합니다.</p>
    <p>설정 메뉴에서 비밀번호를 변경할 수 있습니다.</p>
    <p className="text-[#6b7280]">※ 문의 사항이 있으시면 관리자에게 연락해 주세요.</p>
  </div>
</section>

<hr className="border-[#e5e7eb]" />
```

- [ ] **Step 3: 본문 콘텐츠 — 섹션 2 (감독교사)**

```tsx
{/* === 섹션 2: 감독교사 === */}
<section className="py-6">
  <SectionHeader id="supervisor" title="✅ 감독교사" refFn={setRef("supervisor")} />

  <SubHeader id="attendance-check" title="출석 체크 방법" refFn={setRef("attendance-check")} />
  <p className="text-sm text-[#6b7280] mb-3">출석부 화면에서 학생 좌석을 터치하여 출결 상태를 변경합니다.</p>
  <div className="bg-white rounded-lg border border-[#e5e7eb] p-4 mb-3">
    <div className="flex flex-wrap gap-x-4 gap-y-2 mb-3">
      <ColorChip color="#22c55e" label="출석 (1번 터치)" />
      <ColorChip color="#f472b6" label="결석 (2번 터치)" />
      <ColorChip color="#3b82f6" label="미체크 (초기 상태)" />
    </div>
    <p className="text-xs text-[#dc2626] font-medium">⚠ 감독 종료 전, 파란색(미체크) 좌석이 없는지 반드시 확인해 주세요.</p>
  </div>

  <SubHeader id="absence-approve" title="불참신청 승인" refFn={setRef("absence-approve")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p>출석부 화면의 <strong>"불참신청"</strong> 탭을 선택합니다.</p>
    <p>학생들의 신청 내역을 확인하고 승인 또는 거절 처리합니다.</p>
  </div>

  <SubHeader id="supervisor-change" title="감독교사 변경" refFn={setRef("supervisor-change")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p><strong>감독일정</strong> 화면에서 자신의 감독 일정을 확인할 수 있습니다.</p>
    <p>변경이 필요한 경우 교체 요청을 할 수 있습니다.</p>
  </div>
</section>

<hr className="border-[#e5e7eb]" />
```

- [ ] **Step 4: 본문 콘텐츠 — 섹션 3 (담임교사)**

```tsx
{/* === 섹션 3: 담임교사 === */}
<section className="py-6">
  <SectionHeader id="homeroom" title="👨‍🏫 담임교사" refFn={setRef("homeroom")} />

  <SubHeader id="weekly-attendance" title="주간 출결 확인" refFn={setRef("weekly-attendance")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p>담임 메인 화면에서 자기 반 학생들의 주간 출결 현황을 확인합니다.</p>
    <p>각 출결 셀에 <strong>호버링(터치)</strong> 하면 출결 기록의 사유를 점검할 수 있습니다.</p>
  </div>

  <SubHeader id="monthly-attendance" title="월간 출결 / Excel 다운로드 / 시수누계" refFn={setRef("monthly-attendance")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p><strong>월간출결</strong> 탭에서 월별 출결 통계를 확인합니다.</p>
    <p><strong>Excel 다운로드</strong> 버튼으로 출결 데이터를 파일로 내려받을 수 있습니다.</p>
    <p><strong>시수누계</strong>(시간) 컬럼으로 학생별 참여시간을 검토합니다.</p>
  </div>

  <SubHeader id="participation-manage" title="참여학생 관리" refFn={setRef("participation-manage")} />
  <div className="text-sm text-[#374151]">
    <p><strong>참여설정</strong> 탭에서 자기 반 학생의 자율학습 참여 여부를 관리합니다.</p>
  </div>

  <SubHeader id="absence-reason" title="불참사유 등록" refFn={setRef("absence-reason")} />
  <div className="text-sm text-[#374151]">
    <p><strong>불참사유</strong> 탭에서 학생별 불참 사유를 등록합니다.</p>
  </div>

  <SubHeader id="homeroom-absence-approve" title="불참신청 승인" refFn={setRef("homeroom-absence-approve")} />
  <div className="text-sm text-[#374151]">
    <p><strong>불참신청</strong> 탭에서 학생들의 신청을 확인하고 승인 또는 거절합니다.</p>
  </div>

  <SubHeader id="homeroom-schedule" title="감독일정 변경" refFn={setRef("homeroom-schedule")} />
  <div className="text-sm text-[#374151]">
    <p><strong>감독일정</strong> 탭에서 자신의 감독 일정을 확인하고 교체 요청을 할 수 있습니다.</p>
  </div>
</section>

<hr className="border-[#e5e7eb]" />
```

- [ ] **Step 5: 본문 콘텐츠 — 섹션 4 (학년관리)**

```tsx
{/* === 섹션 4: 학년관리 === */}
<section className="py-6">
  <SectionHeader id="grade-admin" title="⚙️ 학년관리" refFn={setRef("grade-admin")} />

  <SubHeader id="today-dashboard" title="오늘출결 대시보드" refFn={setRef("today-dashboard")} />
  <div className="text-sm text-[#374151]">
    <p>메인 페이지의 <strong>오늘출결</strong> 탭에서 해당 학년 전체의 실시간 출결 현황을 확인합니다.</p>
  </div>

  <SubHeader id="student-manage" title="학생관리" refFn={setRef("student-manage")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p><strong>학생관리</strong> 탭에서 학생 사용자를 등록하고 수정합니다.</p>
    <p><strong>도우미</strong> 학생을 선정할 수 있습니다. 도우미는 불참신청을 일괄 등록할 수 있는 권한을 가집니다.</p>
  </div>

  <SubHeader id="participation-setting" title="참여설정" refFn={setRef("participation-setting")} />
  <div className="text-sm text-[#374151]">
    <p><strong>참여설정</strong> 탭에서 학년 전체에 대한 자율학습 참여 설정을 관리합니다.</p>
  </div>

  <SubHeader id="seating" title="좌석배치" refFn={setRef("seating")} />
  <div className="text-sm text-[#374151]">
    <p><strong>좌석배치</strong> 탭에서 드래그 앤 드롭으로 학생들의 좌석을 배치합니다.</p>
  </div>

  <SubHeader id="supervisor-assign" title="감독배정" refFn={setRef("supervisor-assign")} />
  <div className="text-sm text-[#374151]">
    <p>월간 캘린더에서 각 날짜에 감독교사를 배정합니다.</p>
  </div>

  <SubHeader id="grade-monthly" title="월간출결 / Excel 다운로드" refFn={setRef("grade-monthly")} />
  <div className="text-sm text-[#374151] space-y-1">
    <p><strong>월간출결</strong> 탭에서 학년 전체 출석 데이터를 조회합니다.</p>
    <p><strong>Excel 다운로드</strong> 버튼으로 데이터를 내려받을 수 있습니다.</p>
  </div>
</section>
```

- [ ] **Step 6: 수동 검증**

```bash
npm run dev
```

아래 항목을 브라우저에서 확인한다:
1. `http://localhost:3000/help` — 로그인 없이 접근 가능한지 확인
2. 헤더 블루 배경 + 포산고 로고 + 타이틀 표시 확인
3. 햄버거 버튼 클릭 → 우측 목차 오버레이 슬라이드 확인
4. 목차 항목 클릭 → 해당 섹션 스크롤 + 사이드바 닫힘 확인
5. 스크롤 시 목차 하이라이트 변경 확인
6. 모바일 뷰(375px) 확인
7. 출석 색상 칩 (초록/핑크/파란) 정상 표시 확인

- [ ] **Step 7: 커밋**

```bash
git add src/app/help/page.tsx
git commit -m "feat: /help 사용설명서 페이지 구현"
```
